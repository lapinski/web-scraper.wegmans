describe('Promise tap helper', () => {
    describe('tap()', () => {
        describe('when given valid inputs', () => {
            it.skip('should pass through the input unchanged.', () => {
            });

            it.skip('should have called the sideffect.', () => {
            });
        });
    });
});