export enum DatabaseType {
  Postgres = 'postgres',
  MySql = 'mysql',
};

export enum LogicalEnvironment {
  Production = 'production',
  Development = 'development',
  Test = 'test',
};

export enum LogLevel {
  Debug = 'debug',
  Info = 'info',
  Warn = 'warn',
  Error = 'error',
};