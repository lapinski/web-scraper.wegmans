version: 2
jobs:
    build:
        docker:
            - image: node:alpine

        working_directory: ~/repo

        steps:
            - checkout
            - restore_cache:
                keys:
                    - v1-dependencies-{{ checksum "package.json" }}
                    - v1-dependencies
            - run: npm ci
            - save_cache:
                paths:
                    - node_modules
                name: v1-dependencies0{{ checksum "package.json" }}
            - run: npm t